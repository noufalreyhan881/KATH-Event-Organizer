import RegistrationSuccessPage from '@/features/competition/pages/RegistrationSuccessPage';

// This makes the page dynamic so it can read search parameters
export const dynamic = 'force-dynamic';

export default function CompetitionRegistrationSuccessPage() {
  return <RegistrationSuccessPage />;
}
